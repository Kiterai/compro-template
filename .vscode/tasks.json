{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "cmake . -B build && cmake --build build",
        },
        {
            "type": "shell",
            "label": "snippet update",
            "command": "cmake . -B build && cmake --build build && ./build/Debug/tool.exe",
        }
    ]
}